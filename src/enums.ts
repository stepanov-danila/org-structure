export const enum StructureType {
  Scheme,
  Table,
}

export const enum LineDirectionEnum {
  Vertical,
  LongVertical,
  Left,
  Right,
}

export const enum SchemeItemEnum {
  Root,
  Supervisor,
  Group,
  SubordinateSupervisor,
  Subordinate,
}

export const enum SchemeItemSideEnum {
  None,
  Left,
  Right,
}

export const enum TableSortDirectionEnum {
  Desc,
  Asc,
}

export const enum TableSortByEnum {
  Name = "name",
  Department = "department",
  PositionRole = "positionRole",
}

export const enum TogglerTypeEnum {
  Expand,
  Collapse,
}
